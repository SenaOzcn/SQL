/*
Who let the dogs out

4967 users solved this problem. Latest completion was less than a minute ago.
Create a table named dogs with three columns: dog_id (INT), name (VARCHAR(20)), and age (INT).
*/

CREATE TABLE dogs(
    dog_id INT,
    name VARCHAR(20),
    age INT
);
